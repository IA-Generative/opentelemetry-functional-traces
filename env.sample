#!/usr/bin/env bash
# Copy to .env and edit before starting
# Comma-separated list of accepted API keys (e.g., for different clients)
API_KEYS=thunderbird_demo_2025,my_secret_key_1234

# Secret salt used to compute HMAC(user_email, HASH_SALT)
HASH_SALT=change-me-super-secret-salt

# Usage:
#   chmod +x env.sample
#   ./env.sample > .env
# Vous pouvez ensuite modifier .env selon vos besoins.
